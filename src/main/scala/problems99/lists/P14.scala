package org.laplacetec.study
package problems99.lists

object P14 {
  /**
   * (*) Duplicate the elements of a list.
   * Example:
   *    scala> duplicate(List('a, 'b, 'c, 'c, 'd))
   *    res0: List[Symbol] = List('a, 'a, 'b, 'b, 'c, 'c, 'c, 'c, 'd, 'd)
   * @tparam A
   * @return
   */
  def solution[A]: List[A] => List[A] = ???
}
